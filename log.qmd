---
title: "Teaching Log"
format: html
---

> Daily updates on progress for the course in each section.


## Copy-Edit Log — Spatial-Points

Tracks copy-editing passes over this module's `.qmd` documents. Clear-cut
typos/grammar/spelling errors are corrected directly in the source. Items
that need an author decision are left in place but wrapped in
`<mark>...</mark>` so they show up highlighted in the rendered HTML.

### narrative.qmd — 2026-08-15

#### Fixed directly (typos, spelling, grammar)

| Line | Issue | Fix |
|---|---|---|
| 70 | "is plot in a specific order" | "is plotted in a specific order" |
| 94 | "tangential ... at example one point" | "at exactly one point" |
| 100 | "it is plot within" | "it is plotted within" |
| 122 | "conecntric circles" | "concentric circles" |
| 133 | "coordiante system" | "coordinate system" |
| 137 | "passes thorugh" | "passes through" |
| 138 | "hemisphers"; "Equator (0° Latitude) and is defined" (stray "and") | "hemispheres"; "Equator (0° Latitude) is defined" |
| 141 | "Universal Trans Mercator" — the correct name, confirmed by the Wikipedia link in the very same sentence | "Universal Transverse Mercator" |
| 141 | "Antartica" | "Antarctica" |
| 158 | "EPGS[^3] registry" (inconsistent with "EPSG" used everywhere else); "publc"; "elipsoids"; missing terminal period | "EPSG[^3] registry"; "public"; "ellipsoids"; period added |
| 207 | "establish home" (missing article); "feamles"; "phermones" | "establish a home"; "females"; "pheromones" |
| 242 | "able to intrepret properly" | "able to interpret properly" |
| 272 | "This additional `sf` attributes gives" (agreement); "poitns" | "These additional `sf` attributes give"; "points" |
| 284 | "poitns" | "points" |
| 293 | "the center of the all the points." (duplicate "the"; lowercase, inconsistent with sibling fragments) | "The center of all the points." |
| 300 | lowercase start, inconsistent with sibling fragments | "And the area enclosed..." |
| 312 | "GRS80 elipsoid" | "GRS80 ellipsoid" |
| 327 | "a `sf` data set" (a/an) | "an `sf` data set" |
| 339 | "names of dat columns" | "names of data columns" |
| 345 | "coordiantes"; "aare taken care of"; "definining" | "coordinates"; "are taken care of"; "defining" |
| 352 | "the same ways for lables" | "the same way for labels" |
| 365 | "similiarly" | "similarly" |
| 374 | "these series of coordiantes" (agreement) | "this series of coordinates" |
| footnote 2 | link text says "proj.org" but the href pointed to `https://prog.org` | href corrected to `https://proj.org` to match the visible text |
| footnote 3 | `[https://en.wikipedia.org/...](European Petroleum Survey Group)` — link text and URL were swapped | `[European Petroleum Survey Group](https://en.wikipedia.org/...)` |
| footnote 5 | "everything is plot in" | "everything is plotted in" |

#### Flagged for your review

None — every issue found was an unambiguous typo, grammar slip, or markup bug with an obvious fix.
